<div class='container'>
 <h2>注文一覧</h2>

    <table class="table table-striped">

     <thead>
      <tr>
       <th>購入日時</th>
       <th>購入者</th>
       <th>注文個数</th>
       <th>注文ステータス</th>
     </thead>
     <tbody>
      <% @orders.each do |order| %>
      <tr>
        <td><%= l oerder.created_at%></td>
        <td><%= order.customer.name%></td>
        <td><%= oerder.created_at.count %></td>
        <td><%= order.order_status%></td>
       </tr>
       <% end %>
      </tbody>
     </table>
     <%= paginate  @orders %>
</div>