<h2>注文情報確認</h2>
<table class="table table-bordered table-layout:fixed width:100%">
    <thead>
      <tr>
        <td style="width: 20%">商品</td>
        <td style="width: 30%">単価(税込み)</td>
        <td style="width: 20%">数量</td>
        <td style="width: 30%">小計</td>
      </tr>
    </thead>
    <tbody>
      <% current_customer.cart_products.each do |cart_product| %>
        <tr>
          <td><%= cart_product.product.name %></td>
          <td><%= cart_product.product.price * 1.1 %></td>
          <td><%= cart_product.amount %></td>
          <td><%= cart_product.product.price * 1.1 * cart_product.amount %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<h2>支払情報</h2><%= @order.payment_method %>
<h2>お届け先</h2>〒<%= @order.postal_code %><%= @order.address %><%= @order.name %>
<h2>送料</h2><%= @order.freight %>
<h2>商品合計</h2><%= @order.billing_amount %>
<h2>請求額</h2><%= @order.freight + @order.billing_amount %>
<%=link_to "注文を確定する", orders_complete_path(params[:id]) %>