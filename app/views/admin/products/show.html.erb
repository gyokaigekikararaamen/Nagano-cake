<div class='container'>
 <p class=" title text-left mt-5 w-25 text-center" ><strong>商品詳細</strong></p>
 <div class='row mt-5'>
  
   <div class='col-md-4 mr-4'>
    <p class='mr-4'> <%= attachment_image_tag @product, :image,  format: 'image', size: "350x200" %></p>
   </div> 
   <div class="col-md-7">    
     <table class="ml-5">
       <tr>
        <th class="d-inline-block mt-2 mr-5">商品名</th>
        <td class="d-inline-block mt-2 	ml-4 position:relative"><%= @product.name %></td>
       </tr> 
       <tr>
        <th class="d-inline-block mt-3 mr-5">商品説明</th>
        <td class="d-inline-block mt-3 ml-2 position:absolute"><%= @product.description %></td>
       </tr> 
       <tr>
        <th class="d-inline-block mt-5 mr-5">ジャンル</th>
        <td class="d-inline-block mt-5 ml-2 position:absolute"><%= @product.genre.name%></td>
       </tr>
       <tr>
        <th class="d-inline-block mt-3 mr-5">税込価格</th>
        <td class="d-inline-block mt-3 ml-2 position:absolute"><%= @product.add_tax_price.to_s(:dalimited) %>（<%= @product.price %>）円</td>
       </tr>
       <tr>
        <th class="d-inline-block mt-3 mr-3">販売ステータス</th>
        <% if @product.product_status =="販売中"%>
        <td class="d-inline-block text-success font-weight-bold mt-3 ml-2 position:absolute"><%= @product.product_status %></td>
        <% else %>
        <td class="d-inline-block mt-3 ml-2 position:absolute"><%= @product.product_status %></td>
        <% end %>
       </tr>
      </table> 
    </div>   
   <p class="mx-auto mt-5"><%=link_to "編集する",  edit_admin_product_path(@product.id),class:"btn btn-outline-secondary"%><p>
 </div>    
</div>     
     