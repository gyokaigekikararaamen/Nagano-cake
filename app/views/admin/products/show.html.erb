<div class='container'>
  <h4 class=" bg-light text-left mt-5 w-25 text-center" >商品詳細</h4>
 <div class='row mt-5'>
  
   <div class='col-md-4'>
    <p> <%= attachment_image_tag @product, :image,  format: 'image', size: "350x200" %></p>
   </div> 
   <div class="col-md-8">    
     <table  >
       <tr>
        <td>商品名</td>
        <td class="d-inline-block  ml-2"><%= @product.name %></td>
       </tr> 
       <tr>
        <td>商品説明</td>
        <td class="d-inline-block mt-1 ml-2"><%= @product.description %></td>
       </tr> 
       <tr>
        <td>ジャンル</td>
        <td class="d-inline-block mt-1  ml-2"><%= @product.genre.name%></td>
       </tr>
       <tr>
        <td>税込価格</td>
        <td class="d-inline-block mt-1  ml-2"><%= @product.add_tax_price.to_s(:dalimited) %>（<%= @product.price %>）円</td>
       </tr>
       <tr>
        <td>販売ステータス</td>
        <td class="d-inline-block mt-1  ml-2"><%= @product.product_status %></td>
       </tr>
      </table> 
     <%=link_to "編集する",  edit_admin_product_path(@product.id),class:"btn btn-success"%> 
  </div>   
 </div>    
</div>     
     